import { defineConfig } from "vite";

// For example, if your repo is "tank-party", use: base: "/tank-party/"
// For now, using "./" works locally and for other static hosting.

export default defineConfig({
  base: "/living-room-games",
});
